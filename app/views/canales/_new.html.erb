<%= form_with(model: @canal, local: true) do |form| %>

	<table>
		<thead>
			<tr>
				<th></th>
				<th>Nuevo canal</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td>
					<div class="field">
						<%= render 'layouts/mensajes' %>
					</div>
				</td>
				<td>
					<div class="field">
						<%= form.text_field :nombre, placeholder: "Escriba un nombre" %>
					</div>
				</td>
				<td>
					<div class="">
						<%= form.button "Guardar", class: "btn btn-outline-warning letra-negra btn-sm mb-3 mx-1" %>
					</div>
				</td>
			</tr>
		</tbody>
	</table>

<% end %>
