<%= form_with(model: par, local: true) do |form| %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

<!--   <br>
  <%#= image_tag(par.imagen_url) if par.imagen? %>
  <br>
  <br>
  <div class="field">

    <%= form.file_field :imagen %>
  </div> -->

  <div class="actions">
    <%= form.submit "Guardar" %>
  </div>
<% end %>
